apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: lab-pcc4f-scaledobject
spec:
  scaleTargetRef:
    name: lab-pcc4f
  # how frequently in seconds does KEDA poll Prometheus
  pollingInterval: 30 # Optional. Default: 30 seconds
  cooldownPeriod: 300 # Optional. Default: 300 seconds
  minReplicaCount: 0 # Optional. Default: 0
  maxReplicaCount: 5 # Optional. Default: 100
  triggers:
    - type: aws-sqs-queue
      authenticationRef:
        name: keda-trigger-auth-aws-role
      metadata:
        queueURL: https://sqs.ap-northeast-1.amazonaws.com/045675425505/pcc4f_input_item_id_prod
        queueLength: "100"
        # Required: awsRegion
        awsRegion: "ap-northeast-1"
        identityOwner: pod # Optional. Default: pod
