bases:
  - ../../../../base/search/title-translation
resources:
  - scaledobject.yaml
  - secret.yaml
  - ingress.yaml
  - pdb.yaml
configMapGenerator:
  - name: title-translation-common-env
    behavior: merge
    literals:
      - NODE_ENV=production
  - name: title-translation-nginx-conf
    behavior: merge
    files:
      - configmap/title-translation.conf
patchesStrategicMerge:
  - deployment_patch.yaml
  - service_patch.yaml
