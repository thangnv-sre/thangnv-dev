bases:
  - ../../../../base/contributor/footage-resize-demand
resources:
  - secret.yaml
  - scaledobject.yaml
configMapGenerator:
  - name: footage-resize-demand-common-env
    behavior: merge
    literals:
      - RESIZE_ENV=production
patchesStrategicMerge:
  - deployment_patch.yaml
