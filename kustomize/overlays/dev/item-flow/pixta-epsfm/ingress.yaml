apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: epsfm-ingress-external
  annotations:
    kubernetes.io/ingress.class: nginx-external
spec:
  rules:
    - host: epsfm-staging.pixtavietnam.com
      http:
        paths:
          - backend:
              serviceName: epsfm-svc
              servicePort: 80
