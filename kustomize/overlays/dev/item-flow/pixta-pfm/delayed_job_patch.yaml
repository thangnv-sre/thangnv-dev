apiVersion: apps/v1
kind: Deployment
metadata:
  name: pfm-delayed-job
  labels:
    app: pfm-delayed-job
    stage: staging
spec:
  replicas: 0
  selector:
    matchLabels:
      app: pfm-delayed-job
      stage: staging
  template:
    metadata:
      annotations:
        iam.amazonaws.com/role: arn:aws:iam::045675425505:role/pixtavietnam-staging
      labels:
        app: pfm-delayed-job
        stage: staging
