apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: pixta-mass-mailer-ingress-external
  annotations:
    kubernetes.io/ingress.class: nginx-external
spec:
  rules:
    - host: massmailer-staging.pixta.jp
      http:
        paths:
          - backend:
              serviceName: pixta-mass-mailer-svc
              servicePort: 80
