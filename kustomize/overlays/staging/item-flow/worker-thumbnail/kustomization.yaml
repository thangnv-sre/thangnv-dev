bases:
  - ../../../../base/item-flow/worker-thumbnail
resources:
  # - hpa.yaml
  - secret.yaml
  - scaledobject.yaml
configMapGenerator:
  - name: worker-thumbnail-common-env
    behavior: merge
    literals:
      - RAILS_ENV=staging
patchesStrategicMerge:
  - deployment_patch.yaml
