bases:
  - ../../../../base/item-flow/worker-product-reserved
resources:
  - scaledobject.yaml
  - secret.yaml
configMapGenerator:
  - name: worker-product-reserved-common-env
    behavior: merge
    literals:
      - RAILS_ENV=staging
patchesStrategicMerge:
  - deployment_patch.yaml
