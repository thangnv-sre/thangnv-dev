apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ffm-ingress-external
  annotations:
    kubernetes.io/ingress.class: nginx-external
spec:
  rules:
    - host: ffm-staging.pixtavietnam.com
      http:
        paths:
          - backend:
              serviceName: ffm-svc
              servicePort: 80
