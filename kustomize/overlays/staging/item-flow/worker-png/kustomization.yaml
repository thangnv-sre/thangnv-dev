bases:
  - ../../../../base/item-flow/worker-png
resources:
  - secret.yaml
  # - scaledobject.yaml
configMapGenerator:
  - name: worker-png-common-env
    behavior: merge
    literals:
      - RAILS_ENV=staging
patchesStrategicMerge:
  - deployment_patch.yaml
