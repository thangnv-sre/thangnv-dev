apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: argocd

# Example configuration for the webserver
# at https://github.com/monopole/hello
commonLabels:
  app: argocd-dev

resources:
- nginx-deployment.yml
- Secret-aws-class.yml